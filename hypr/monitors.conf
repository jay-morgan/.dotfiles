######### BELOW IS THE DEFAULT  ############
# See https://wiki.hyprland.org/Configuring/Monitors/
# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale

# Optimized for retina-class 2x displays, like 13" 2.8K, 27" 5K, 32" 6K.
env = GDK_SCALE,2
# monitor=,preferred,auto,auto

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.6

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2

#### CUSTOM SETTINGS START ####

# Framework 13 - scale 3 for multi-monitor (hotplug script switches to 2 when solo)
monitor = eDP-1, 2880x1920@120, 0x0, 3

# External monitors (desc: matching so positions apply atomically on reload).
# auto-right chains each offset from the scaled width to its left, so a scale
# change (e.g. solo fallback flips eDP to 2x) can never leave a pinned x= overlapping.
monitor = desc:Dell Inc. DELL U2723QE, 3840x2160@60, auto-right, 2
monitor = desc:Dell Inc. DELL U2421E, 1920x1200@60, auto-right, 1.2, transform, 1
